<?xml version="1.0" encoding="UTF-8"?>  
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security.xsd">

	<context:component-scan base-package="com.sp"/>
	
    <!-- 권한이 없는 페이지를 접근할 경우 접근 불가 메시지 출력  -->
	<http auto-config="true" use-expressions="true">
		<!-- spring 4.x때 추가된 옵션으로 ssl을 사용하지 않을 경우 csrf는 disalbed=true로 설정. -->
		<csrf disabled="true"/>
		
		<headers>
			<frame-options policy="SAMEORIGIN"/>
			<hsts disabled="true"/>
		</headers>
		
		<intercept-url pattern="/index.jsp" access="permitAll" />
		<intercept-url pattern="/main" access="permitAll" />
		<intercept-url pattern="/travel" access="permitAll" />
		<intercept-url pattern="/travel/location" access="permitAll" />
		<intercept-url pattern="/travel/location/recommendLocation" access="permitAll" />
		<intercept-url pattern="/travel/location/view" access="permitAll" />
		<intercept-url pattern="/travel/location/replyList" access="permitAll" />
		<intercept-url pattern="/travel/landmark" access="permitAll" />
		<intercept-url pattern="/travel/landmark/view" access="permitAll" />
		<intercept-url pattern="/travel/landmark/replyList" access="permitAll" />
		<intercept-url pattern="/travel/party" access="permitAll" />
		<intercept-url pattern="/travel/party/view" access="permitAll" />	
		<intercept-url pattern="/member/login" access="permitAll" />
		<intercept-url pattern="/member/join" access="permitAll" />
		<intercept-url pattern="/member/step1" access="permitAll" />
		<intercept-url pattern="/member/step2" access="permitAll" />
		<intercept-url pattern="/member/step3" access="permitAll" />
		<intercept-url pattern="/member/complete" access="permitAll" />
		<intercept-url pattern="/member/idCheck" access="permitAll" />
		<intercept-url pattern="/member/emailCheck" access="permitAll" />
		<intercept-url pattern="/resources/**" access="permitAll" />
		<intercept-url pattern="/uploads/**" access="permitAll" />
		<!-- <intercept-url pattern="/" access="permitAll" /> -->
		<intercept-url pattern="/**" access="hasRole('ROLE_USER')" />
		
		<form-login
			login-page="/member/login"
			login-processing-url="/member/login"
			username-parameter="userId"
			password-parameter="userPwd"
			always-use-default-target="false"
			authentication-failure-url="/member/login?login_error"
			authentication-success-handler-ref="loginSuccessHandler"
		/>
		
		<logout
			logout-url="/member/logout"
			invalidate-session="true"
			logout-success-url="/main"
		/>
		
		<access-denied-handler error-page="/member/noAuthorized"/>
		
		<session-management>
			<concurrency-control expired-url="/member/expired" max-sessions="1" />
		</session-management>
		
		<custom-filter ref="ajaxFilter" after="EXCEPTION_TRANSLATION_FILTER" />
	</http>
	
	<beans:bean id="loginSuccessHandler" class="com.sp.security.LoginSuccessHandler"/>
	    
	<jdbc-user-service data-source-ref="dataSource"
		id="userService"
		users-by-username-query="select userId as username, userPwd as password, enabled from tb_user where userId=?"
		authorities-by-username-query="select userId username, authority from tb_user u join identifierCode i on u.identCode = i.identCode where userId=?"
	/>    
	
	<!-- <beans:bean id="bcryptEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />    
	    
	<authentication-manager>
		<authentication-provider user-service-ref="userService">
			<password-encoder ref="bcryptEncoder"/>
		</authentication-provider>
	</authentication-manager>
	 -->
	<beans:bean id="ajaxFilter" class="com.sp.security.AjaxFilter">
		<beans:property name="ajaxHeader" value="AJAX"/>
	</beans:bean>
</beans:beans>
